import { useSocket } from "../context/socketContext";

function RoomsList() {
  return (
    <div>
      <input type="text" />
      <button>Skapa rum</button>
    </div>
  );
}

export default RoomsList;
